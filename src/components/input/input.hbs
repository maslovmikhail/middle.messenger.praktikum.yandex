<label class="input__container">
  <input
    class="input__element input__{{decoration}}"
    id="{{id}}"
    placeholder="{{placeholder}}"
    type="{{type}}"
    value="{{value}}"
    name="{{name}}"
    decoration="primary"
  />
  <div class="input__label">{{label}}</div>
</label>